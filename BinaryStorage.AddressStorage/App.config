<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <appSettings>
    <!-- this exists simply to facilitate testing.  real use will be from an executable, from which the runtime will
         pull the executable app's config value.  Since a unit test is a class library, usually, the code needs this app.config file or it will fail.
         -->
    <add key="dataStoragePath" value="f:\datastores\testaddresses.dat"/>
  </appSettings>
    <startup> 
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.7"/>
    </startup>
</configuration>
